#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory

( cd buoyantSimpleRadiationFoam && wclean )
( cd buoyantBoussinesqSimpleFoam && wclean )


# ----------------------------------------------------------------- end-of-file
