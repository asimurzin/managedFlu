#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory

( cd boundaryFoam && wmake )
( cd channelFoam && wmake )
( cd icoFoam && wmake )
( cd nonNewtonianIcoFoam && wmake )
( cd simpleFoam && wmake )


# ----------------------------------------------------------------- end-of-file
